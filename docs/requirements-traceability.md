# Melbourne Sphere — SRS traceability

Maps SRS sections/requirements to implementation status, modules and evidence. Updated at the end of every phase. Status values: **done**, **partial**, **planned**, **blocked (decision)**, **future** (explicitly out of MVP).

| SRS section / IDs | Status | Modules / files | Evidence (tests, checks, runtime) | Remaining work |
| --- | --- | --- | --- | --- |
| 1 Scope — SCP 001–005 | partial | server-owned Melbourne constants (`apps/api/src/site/site.controller.ts`); local-area allowlist with council-area baseline seed (`taxonomy/seed-data.ts`); no city/tenancy code; D01 recommendation and tiered locality allow-list in `docs/launch/melbourne-boundary.md` (nothing activated; council-area baseline unchanged) | Phase 11 integration tests (no expansion fields accepted); Phase 12 eligibility gate (`eligibilityVerifiedAt` required to publish, area must be active) | D01 boundary decision (pending client approval of the recommendation, 2026-09-07); QA T01; rules 5–7 of the boundary document once D01 is recorded |
| 2 Public experience — UX 001–003 | partial | `packages/ui/src/styles.css` (light-first token system: colours, type, content widths, spacing, radii, shadows, focus rings, motion), `apps/web/src/components/page-shell.tsx` (full-bleed `Band` + bounded `.ms-container`), redesigned shell (`site-header.tsx` with mobile `<details>` navigation and the listing action, `site-footer.tsx`), redesigned home (`app/page.tsx`), cards (`business-card.tsx`, `post-card.tsx`, `featured-post-card.tsx`, `category-icon.tsx`); admin theme tokens (`apps/admin/src/config/theme.ts`) | `palette.test.ts` (AA contrast for every light surface and dark band, and a guard against reintroducing an OS dark scheme), `site.test.ts`, e2e `accessibility.spec.ts` (axe WCAG 2.2 AA scan of `/`, `/business`, `/blog` and a listing at desktop and 320 px; mobile menu), browser review at 1440/1024/768/390 px | About appears in the navigation once that information page is published (Contact always resolves); brand logotype and client photography |
| 3 Architecture — ARC 001–005 | partial | pnpm workspace; `apps/web`, `apps/api`, `apps/admin`, `apps/worker`, `packages/{database,contracts,ui,domain}`; same-origin `/api/v1` proxies; Refine vertical slice | `pnpm check` (all workspaces build), bundle scans (no Refine/antd in public chunks), worker runtime check | `packages/config`; S3 storage adapter (phase 20) |
| 4 Hero — HERO 001–007 | done (interim photography) | `apps/web/src/components/hero-banner.tsx` (full-bleed photograph, directional wash on `sm`+ and an even tint below it, per-slide focal points, cross-fade with previous/next/pause/per-image controls, designed gradient fallback), `hero-headline.tsx` (display-serif H1, reserved-height rotating phrase), `hero-search.tsx` (rounded panel: fixed Melbourne label, labelled keyword combobox, category select, prominent Search), `lib/hero-assets.ts` (licensed default slides), `apps/api/src/settings/*` | `hero.test.ts`, `hero-banner.test.tsx` (axe, controls, decorative images), `settings-suggestions.integration-spec.ts`, `home-settings.spec.ts`, e2e keyboard search + axe, browser review at four widths | Client's own photography replaces `DEFAULT_HERO_SLIDES` (see `docs/content/hero-photography.md`); counters stay hidden until an admin enables them |
| 5 Discovery — DIR 001–007 | partial | `apps/api/src/business/search/*` (SearchService, ranking, facets), `dto/public-business.dto.ts`, `directory-public.controller.ts`; web `directory-results.tsx`, `search-params.ts`, filters/chips/pagination, listing cards with a category-derived branded fallback instead of one shared placeholder (`business-card.tsx`, `lib/category-visuals.ts`); DIR 007 featured block (`featured.service.ts`, at most three matching published listings in `meta.featured`, excluded from organic results and pagination, labelled block on the web) | `search.integration-spec.ts`, `search-rules.spec.ts`, `search-params.test.ts`, `featured.integration-spec.ts`, `category-visuals.test.ts`, runtime checks | DIR 008 "Open now" **implemented conditionally**: SQL predicate in `directory/hours/open-now.ts` proven to agree case-for-case with `evaluateHours` (`open-now.integration-spec.ts`), offered only when published hours coverage reaches the documented threshold (`meta.openNow.available`), ignored below it, and never including a listing without published hours; suggestions delivered in phase 15 (`suggestions.service.ts`) |
| 6 Listings — BUS 001–008 | partial | BUS 002/006/007 core in `apps/api/src/business/` (rules, service, admin controller), schema `businesses*` tables; BUS 003 phone/link rules (`business-rules.ts`); BUS 004 hours (`directory/hours/*`, `opening_intervals`, `hours_exceptions`); BUS 008 local areas (`taxonomy.service.ts`); admin `pages/businesses/*` incl. `HoursEditor` | Phase 12: `directory.integration-spec.ts`, `business-rules.spec.ts`, admin `BusinessesPage.test.tsx`; Phase 13: `melbourne-time.spec.ts`, `hours-rules.spec.ts` (DST), `hours.integration-spec.ts`, runtime curl + browser | gallery (phase 20); BUS 001 public presentation, BUS 003 contact actions and BUS 005 related listings delivered in phase 14 (`apps/web/src/app/business/[slug]/page.tsx`, `hours-table.tsx`) |
| 7 Reviews/reports — REV 001–005, REP 001–002 | done (reports cover comments too) | `apps/api/src/reviews/*` (submission guards, moderation, aggregates, reports), `common/captcha/*`, `common/idempotency.service.ts`, `reviews`/`abuse_reports`/`idempotency_records`; admin `pages/moderation/*`; web `review-form.tsx`, `review-list.tsx`, `rating-panel.tsx`/`rating-stars.tsx` (average, partial-fill stars and the approved-review distribution `ratingBreakdown`, computed over every approved review rather than the page on screen) | `reviews.integration-spec.ts` (16 assertions groups), `review-rules.spec.ts`, admin `Moderation.test.tsx`, web `submissions.test.ts` and `rating-panel.test.tsx` (axe), `reviews.integration-spec.ts` distribution case, runtime fail-safe check | retention purges (phase 22); real Turnstile keys (D03) |
| 8 Enquiries — ENQ 001–007 | mostly complete (provider account pending) | `apps/api/src/enquiries/*`, `outbox/*`, `apps/worker/*`, `packages/domain/*`, tables `enquiries`/`outbox_events`/`provider_message_events`; admin `pages/enquiries/*`; web `enquiry-form.tsx` and `contact-form.tsx` (the `/contact` page posts the general enquiry through the same pipeline, arriving in the queue as `kind: site`); **transactional email boundary** `packages/mail` (SMTP transport on nodemailer: bounded DNS/connection/greeting/socket timeouts, no pooling, plain text only, header-injection refusal, caller-owned stable `Message-ID`, transient/permanent classification, addresses redacted from errors), `apps/api/src/auth/mailer/smtp-mailer.ts`, `apps/worker/src/mailer/smtp-mailer.ts`; production refuses to start unless `MAIL_TRANSPORT=smtp` with an authenticated TLS non-loopback relay and a verified sender; Mailpit catcher in Compose | `enquiries.integration-spec.ts`, `enquiry-delivery.spec.ts`, `enquiry-mail.spec.ts`, worker `config.spec.ts`, admin `EnquiriesPage.test.tsx`, runtime end-to-end delivery; `packages/mail` `config.spec.ts`/`transport.spec.ts`, API `env.validation.spec.ts` (smtp rules, no value echoed), worker `config.spec.ts`/`smtp-mailer.spec.ts`; runtime 2026-09-07: password reset and two contact enquiries delivered through the SMTP adapter into Mailpit with Reply-To and `Message-ID`, `providerAccepted` recorded, replayed idempotency key sent once, closed relay classified transient in 5 ms; mail-package security audit tests (TLS 1.2 floor with verification on, multi-recipient/display-name/bracketed addresses refused for to/from/replyTo, subject 998 and body 256 KB bounds, plain text only with no html or attachments, verify() classification, transport close, Message-ID stable across retries, errors free of body/subject/address/credential); runtime 2026-09-07 through the ignored local env: reset and enquiry mail in Mailpit, one enquiry message for a replayed idempotency key, no token in the console | ENQ 006 webhook ingestion and bounce handling (provider-specific; after D03b); provider credentials and verified sender (client, D03b/D04) |
| 9 Blog — BLOG 001–005, COM 001–002 | done | `apps/api/src/blog/*` (sanitiser, editorial service, public reads, comments), tables `posts`/`authors`/`blog_categories`/`blog_tags`/`post_tags`/`content_revisions`/`comments`; admin `pages/blog/*` and `pages/moderation/CommentsPage`; web `app/blog/**`; full author profiles (`authors` + `author_links`, `blog/author-rules.ts`), rich-text article bodies (`bodyFormat`, TipTap editor in `admin/src/components/RichTextEditor.tsx`) sanitised by the same allowlist, article cover media, public byline and author card (`web/src/components/author-byline.tsx`, `author-card.tsx`); public presentation reworked 12 Sep 2026 — one canonical `post-card.tsx` (standard/featured), `article-media.tsx` (reserved ratio, rendition choice, one fallback for both "no cover" and "cover will not load"), `article-collection.tsx`, `blog-category-nav.tsx`, a 46rem reading column (`.ms-container-read`) and `.ms-prose-article` | `blog.integration-spec.ts`, `blog-public.integration-spec.ts`, `sanitise.spec.ts`, `post-rules.spec.ts`, `share.test.ts`, web `article-media.test.tsx`/`post-card.test.tsx`/`article-collection.test.tsx`/`blog-category-nav.test.tsx`/`comment-form.test.tsx`, admin `Blog.test.tsx`/`CommentsPage.test.tsx`, runtime route and header checks, `authors.integration-spec.ts`, `author-rules.spec.ts`, admin `Authors.test.tsx`, e2e `editorial.spec.ts` + axe scan of `/blog`, browser review of the index, archives and article at 375/430/768/1024/1280/1440 px with axe | cover images (phase 20); sitemap and JSON-LD (phase 21); cache purge (phase 22) |
| 10 Admin/auth — ADM 001–003, AUTH 001–003, RBAC 001 | partial | as before, plus ADM 003 dashboard aggregates scoped to the caller's permissions with no private text (`apps/api/src/dashboard/*`, `admin/src/pages/DashboardPage.tsx`), RBAC 001 extended with `redirects.manage`, and the ADM 002 screen set now covering listings, taxonomy, media, articles, authors, editorial taxonomy, enquiries, moderation queues, settings, SEO redirects, administrators, account security and audit log on one design system (grouped navigation, `PageHeader`/`SectionCard`/`StatCard`/`StatusTag`/`EmptyState`/sticky save bar) | API integration 101 + e2e 19 + unit 141; admin 92 (incl. `Dashboard.test.tsx`, `Authors.test.tsx`, `Redirects.test.tsx`); `dashboard.integration-spec.ts` permission scoping | remaining screen-by-screen polish (phase 22), mandatory-TOTP policy (D06), audited second-factor reset action |
| 10 Access control — RBAC 002–012 (SRS 1.1) | done (audited and closed 2026-09-07) | `apps/api/src/authorization/*` (effective-permission resolver with Redis caching keyed by an authorization version, CASL ability factory, access administration service and API), `apps/api/src/identity/permissions.ts` (code-declared catalogue with labels, modules, active/system state and the `admin:seed-rbac` synchronisation), `apps/api/src/auth/guards/permissions.guard.ts` (default deny through CASL), schema `roles.isActive/version`, `permissions.label/module/isActive/isSystem`, `admin_permissions` (direct grants), `admin_users.authzVersion`, `audit_logs.userAgent` (migration `20260907082702_authorization_roles_and_direct_permissions`); admin `src/auth/{permissions.ts,access-control.ts,CapabilityProvider.tsx,RequirePermission.tsx}` (Refine `accessControlProvider`, typed codes, one route mapping), `src/pages/access/*` (roles list, role editor with a grouped permission matrix, read-only catalogue, administrator access editor showing inherited vs direct and the source of each capability), `ForbiddenPage` | `authorization.integration-spec.ts` (21 cases against real MySQL/Redis: inherited, multi-role, direct, duplicate, inactive role/permission/administrator, no-grant; 401/403 on routes reached directly; self-edit, grant-beyond-own, last-super-admin, protected-role, role-in-use and stale-version refusals; audit written and rolled back with its change; cache hit, version bust and Redis-outage fallback), `ability.factory.spec.ts` (CASL `manage` wildcard trap), `permissions.guard.spec.ts`, `permissions.spec.ts`, admin `access-control.test.tsx` (navigation visibility, forbidden route, loading state), `RolesPage.test.tsx`, `permissions.test.ts` (catalogue drift), `Accessibility.test.tsx` (axe over the role editor and access editor), browser check of the roles, catalogue, role editor and access screens at 1280 and 390 px **Audit 2026-09-07** (`docs/audits/authorization-integration-audit.md`): six defects found and fixed — self-escalation through `PATCH/POST /admin/admins` (critical), a non-atomic last-super-admin check that two concurrent requests could break (high), inactive roles assignable through the account endpoint (high), privilege changes not revoking sessions contrary to AUTH 002 (high), the frontend capability cache surviving logout (medium) and raw permission strings in eight screens (medium). New evidence: `authorization-audit.integration-spec.ts` (21 attack scenarios + a sweep proving all 137 admin routes deny by default), `authorization-schema.integration-spec.ts` (8 constraint proofs against real MySQL), `capability-lifecycle.test.ts`, `ActionGating.test.tsx`, and a five-persona runtime matrix with 49 ms live revocation. **Closure 2026-09-07** (audit addendum): privileged-mutation ceiling (20/min, 200/h per administrator, keyed on the authenticated id, per-process fallback during a Redis outage, `429` with `Retry-After`) proven by `sensitive-throttle.service.spec.ts` and an integration test; the four credential-gated Playwright journeys replaced by five that run on desktop and at 320 px with self-provisioned single-use administrators (`e2e/specs/{authorization.spec.ts,provisioning.ts}`, suite now **44 passed / 0 skipped**); verified MySQL TLS implemented and required in production (`url.spec.ts`, `env.validation.spec.ts`); operations runbook and `pnpm --filter api authz:verify` for the supported-change rule and emergency intervention | explicit deny semantics deliberately absent (RBAC 004) and would need an SRS revision; row-level policies would add `@casl/prisma` when a requirement exists |
| 16 Site configuration — CFG 001–002 | done | home/hero settings incl. the banner slides and counters (`apps/api/src/settings/*`), the five information pages with sanitised rich content, revisions and a publication gate refusing stub or placeholder copy and unvalidated contact routing; **general settings** (`settings/general-settings.ts`, `dto/general-settings.dto.ts`, admin `GeneralSettingsPage.tsx`): application and organisation name, tagline, default meta description, support email (a development domain is refused) and Australian phone, postal address, logo/browser icon/share image from the media library, the header contact bar, one profile URL per social platform (Facebook, Instagram, X, YouTube, **Pinterest**) each checked against that platform's own domain, and the footer copyright template ({year}/{name}) and text; consumed by the public shell (`site-header.tsx`, `site-footer.tsx`, `social-links.tsx`, `lib/copyright.ts`, `generateMetadata`), which omits anything unset instead of rendering a blank; admin screens for all three; public `/{slug}` pages on a shared `InformationPage` layout and footer links only when published; `/contact` always resolves — published copy when it exists, otherwise a factual explanation of how to reach the editors, `noindex` until the approved copy is published | `general-settings.spec.ts` (15 cases: normalisation, development-domain refusal, per-platform host checks, copyright placeholders), `settings-suggestions.integration-spec.ts` (permissions, validation, versioning, audit metadata without values, public payload), `static-pages.integration-spec.ts`, `static-pages.spec.ts`, admin `GeneralSettingsPage.test.tsx` and `SiteSettingsPage.test.tsx`, web `copyright.test.ts`, `site.test.ts`, `social-links.test.tsx`, runtime check of the rendered header/footer | brand logotype and the approved public contact address remain client decisions; until an address is published `contactChannel()` withholds development domains and the listing action points at the homepage explanation |
| 12 SEO — SEO 001–007 | mostly complete | server-rendered pages with one H1, title template, absolute canonical (`SITE_ORIGIN`), tracking parameters stripped, `noindex, follow` on filtered searches, crawlable prev/next; `robots.txt`, `/sitemap.xml` index with per-section children (`apps/web/src/app/sitemap.xml`, `sitemaps/[section]`), JSON-LD builders with script-safe escaping (`apps/web/src/lib/structured-data.ts`), redirect table and `middleware.ts` (301/410, no chains or cycles); SEO 003 substantive-landing rule on directory category and area pages (`landingRobots`: indexed only with editorial text and ≥1 listing); SEO 006 `AggregateRating` gated behind `REVIEW_RICH_RESULTS` (default off); proposals and sign-off table in `docs/launch/seo-approval.md` | `seo.integration-spec.ts`, `structured-data.test.ts`, `pnpm check` | review rich results held for the technical lead (flag off; sign-off record in the SEO pack); client approval of titles/descriptions (S01); WebSite SearchAction (proposal P5) |
| 13 Data model — DAT 001–003 | partial | Prisma 7 foundation, `SystemProbe`, utf8mb4 + `utf8mb4_unicode_ci` policy (migration `collation_policy_*`, `check-migrations.mjs`) | Phase 6/8 integration tests; `pnpm db:migrations:check` | product entities |
| 14 Editorial/operational model — DAT 004–006 | partial | Post (with `bodyFormat`, `coverMediaId`), full Author profile plus `author_links`, BlogCategory, Tag, ContentRevision, Comment, MediaAsset/MediaVariant, `redirects`, outbox and idempotency records; versioned migrations and the policy lint | `blog.integration-spec.ts` (unique slugs, transactional publication, revisions), migration checks | StaticPage and SeoMetadata (phase 22) |
| 15 REST contract — API 001–004 | partial | as before plus `GET/POST /businesses/{id}/reviews`, `POST /reports`, `GET /home`, `GET /search/suggestions`; API 003 idempotency (`idempotency_records`, `Idempotency-Key`, replay receipts) | API e2e (19), `search.integration-spec.ts`, `reviews.integration-spec.ts`, `pnpm check` | enquiry, blog and static-page routes (phases 17–19, 22) |
| 16 Admin API — API 005, MOD 001–002 | partial | `/api/v1/admin/auth/*` (AuthenticationModule), IdentityModule, AuditModule; default-deny guard chain; readiness for MySQL + Redis | integration tests incl. undeclared-route denial | remaining module families (phases 10–20), expectedVersion on updates |
| 17 Events/caching — EVT 001–002, CACHE 001–003 | done | transactional outbox and dispatcher; BullMQ worker with five attempts and backoff; Redis read cache namespaced by publication version (`apps/api/src/cache/*`), `cache.invalidate` events written in the same transaction as every publication, featuring, moderation, page or settings change, worker purge to the web tier (`apps/web/src/app/api/revalidate`) with retries; explicit Next.js revalidate/tags per fetch | `cache-invalidation.integration-spec.ts`, `cache-invalidation.spec.ts` (worker), `queue.spec.ts`, `enquiries.integration-spec.ts` | CDN purge hooks once a CDN exists (phase 24 client decision) |
| 18 Security/privacy — SEC 001–004, PRIV 001–002 | partial | allowlist sanitisation of all editorial content (`blog/sanitise.ts`) protecting public pages and admin previews; server-side Turnstile (`common/captcha/*`), honeypots, public per-IP ceilings (`public-rate-limit.service.ts`), keyed email/IP hashes, encrypted reporter and reviewer emails, acknowledgement version stored; test-artifact revocation (`auth/auth-artifacts.ts`, `cli/revoke-auth-artifacts.ts`, Playwright `global-teardown.ts`) retiring unused reset/set-up links and live sessions, refusing production and unmarked databases | api unit/e2e/integration tests incl. fail-safe 503 paths; scans each phase; `auth-artifacts.integration-spec.ts` (a retired link is refused by the real flow with INVALID_RESET_TOKEN, the session stops authenticating, the pass is idempotent, production and unmarked databases refused); `pnpm --filter api auth:artifacts:check` clean 2026-09-07 | retention jobs (phase 22+), TLS/CSP on web tier (phases 23–24), dependency/container scanning gates (phase 24) |
| 19 NFR 001–013 | mostly complete | axe-core checks on admin screens and the public hero, keyboard/landmark/320 px journeys in Playwright, heading and table-header defects fixed, reduced motion with pause controls, code splitting with an enforced bundle budget, NFR 003 capacity profile passing at 10k businesses / 2k articles / 100k reviews (`tools/load`) after fixing a fixed 10-connection pool and a cache stampede; **waiting and failure are always visible** (NFR 012): a navigation indicator on the public site (`route-progress.tsx`, additive so pages still render without JavaScript), one admin loading screen for route code, session checks and record loads (`components/ui/PageLoader.tsx`) plus a boot loader in `apps/admin/index.html`, a designed 404/500 treatment on the public site (`status-page.tsx`, `error.tsx`, `global-error.tsx`, `global-not-found.tsx`) and an admin boundary that names a stale build and offers the reload that fixes it; brand marks replace link text wherever a profile is shown, each keeping the platform name as its accessible name (`brand-icon.tsx`, admin `BrandIcon.tsx`), and the review rating is a star radio group rather than words | `pnpm check`, `e2e/specs/accessibility.spec.ts` (axe WCAG 2.2 AA over `/`, `/business`, `/blog`, `/contact` and a listing, at desktop and 320 px — zero violations), `status-page.test.tsx`, `route-progress.test.tsx`, `review-form.test.tsx`, `social-links.test.tsx`, admin `ErrorBoundary.test.tsx`, `tools/load/run-load.mjs`, runtime check with the API stopped (real 500 with the shell intact) | manual screen-reader pass; POST-path load measurement on staging |
| 20 Deployment/ops — OPS 001–004, BACK 001–002, MON 001–002 | partial | CI workflow (frozen lockfile, checks, integration services, migrations once, dependency audit), non-root multi-stage Dockerfiles for api/worker/web, `GET /admin/operations/status` signals with MON 002 thresholds, encrypted backup and isolated restore-drill scripts, `docs/operations/runbook.md` and drill log | `operations.integration-spec.ts`; restore drill executed and recorded in `docs/operations/restore-drills.md`; CI runs on the client's infrastructure | environments, region, CDN, error tracking, uptime checks, on-call responder and the first restore drill are client actions |
| 21 Verification — QA 001–003 | mostly complete | Vitest unit/e2e; integration harness against the isolated `_test` MySQL/Redis (API 115, database 5); accessibility specs; Playwright UAT journeys for all four QA 003 flows (`e2e/`); `docs/pre-audit-report.md` | `pnpm check`, `pnpm test:integration`, `pnpm --filter @melbourne-sphere/e2e test` | run the journeys against staging and assemble the signed evidence pack (client) |
| 22 Sequence | n/a | this roadmap | `docs/setup-progress.md` | — |
| 23 Decisions D01–D08 | blocked (decision) | baselines applied: council-area boundary, TOTP optional; register with question, default, alternatives, impact, deadline, status and owner in `docs/launch/client-decisions.md` (D01–D08 plus S01/S02/Q01) | `docs/launch/*` (2026-09-07) | client decisions before launch (all pending; none fabricated) |
| 24 Audit handover | partial | `docs/pre-audit-report.md` (internal pre-audit: verification summary, requirement coverage, security review, open items) | `pnpm check`, `pnpm test:integration` | external audit, UAT evidence pack and signed launch decisions (client) |
| 25 Settings architecture — SET 001–005, RBAC 013 (SRS 1.2) | done (groups declare their settings in their own phases) | `apps/api/src/settings/registry.ts` (four owned groups with per-setting metadata and `enforcedBy`), `settings-store.service.ts` (validate → permission → `expectedVersion` → transaction with audit → declared cache invalidation → envelope; unchanged save is a no-op), `settings-groups.controller.ts` (per-group routes with static permissions, session-only filtered registry), migration `20260907135714_settings_owned_groups` (`security_settings`, `email_settings`, `operations_settings`), `identity/permissions.ts` + `apps/admin/src/auth/permissions.ts` (41 new codes, modules System/Security/Website) | `registry.spec.ts`, `settings-store.service.spec.ts` (12), `settings-groups.integration-spec.ts` (11, real MySQL), admin `permissions.test.ts` contract test, `pnpm --filter api admin:seed-rbac` (41 created) | each group's settings land with their enforcement in Phases 31/33/34–36 |
| 25 Transactional email — MAIL 001–010 (SRS 1.2) | done (provider account pending) | `packages/mail` (`resend-config.ts`, `resend-transport.ts`, `resend-webhook.ts`, shared `maskEmail`), `apps/api/src/email/*` (delivery service, webhook controller, read-only log controller, template vocabulary), `auth/mailer/{resend-mailer,recording-mailer}.ts`, worker `enquiry-delivery.ts` log rows, migration `20260907151409_email_delivery_records`, admin `pages/system/EmailLogsPage.tsx` | mail 32, API unit 217, `email-logs.integration-spec.ts` 13 (real MySQL), worker 22, admin `EmailLogsPage.test.tsx` 4; `docs/operations/email-deliverability.md` | D09 account/domain/DNS/webhook secret and staging verification; the MAIL 010 retention job lands with TASK |
| 25 Activity log — ACT 001–006 (SRS 1.2) | done (retention job scheduled in TASK) | `apps/api/src/audit/activity-catalogue.ts` (domains, categories, derived outcome, retention bound), `audit.controller.ts` at `/admin/activity` with category/outcome/requestId filters, `audit.service.ts` `purgeExpired()`, admin `pages/AuditLogPage.tsx` (Activity log) | `activity-catalogue.spec.ts` 7 (incl. a source sweep proving every written event is categorised), `activity-log.integration-spec.ts` 7 (real MySQL), route-sweep and admins suites on the renamed route | the scheduled job that calls `purgeExpired()` (TASK 001–006) |
| 25 Security settings — SECS 001–008 (SRS 1.2) | done (mandatory TOTP awaits D06) | `apps/api/src/auth/{security-policy,security-consequence,password-history,device-summary}.ts` with enforcement in `session.service.ts`, `auth.service.ts`, `account.service.ts`, `password.service.ts`, `login-throttle.service.ts`; registry group in `settings/registry.ts`; migration `20260907173222_security_password_history`; session routes re-scoped to `security.sessions.*`; admin `pages/security/SecuritySettingsPage.tsx` | `security-settings.integration-spec.ts` 10 (real MySQL), `device-summary.spec.ts` 4, admin `SecuritySettingsPage.test.tsx` 4, three consecutive green integration runs | mandatory two-factor enrolment lands with D06 and its recovery path |
| 25 Cache manager — CMGR 001–005 (SRS 1.2) | done | `apps/api/src/cache/{cache-registry,cache-admin.service,cache-admin.controller}.ts`, admin `pages/system/CacheManagerPage.tsx` | `cache-registry.spec.ts` 6 (incl. a source sweep proving no flush/eval/sendCommand exists), `cache-manager.integration-spec.ts` 8 against real Redis (protected keys proven untouched; patterns refused), admin `CacheManagerPage.test.tsx` 4 | cache warming has no routine to offer yet, so none is declared rather than faked |
| 25 Queue monitor — QMON 001–005 (SRS 1.2) | planned | Phase 35 (BullMQ, worker) | — | per-queue metrics, redacted job detail, bounded actions, multi-replica correctness |
| 25 Scheduled tasks — TASK 001–006 (SRS 1.2) | planned | Phase 36 (BullMQ job schedulers, worker) | — | code registry, distributed lock, missed-run policy, allow-listed run-now |
| 26 FAQs — FAQ 001–005 (SRS 1.2) | done (content pending D10) | `apps/api/src/website/{faq.service,faq.controller}.ts`, `content-support.ts`, migration `20260907161650_website_faqs`, web `app/faqs/page.tsx` + footer link, admin `pages/website/FaqsPage.tsx` | `faqs.integration-spec.ts` 10 (real MySQL), web `faq.test.ts` 2, admin `FaqsPage.test.tsx` 3, runtime check of `/faqs` | client FAQ copy (D10); `FAQ_RICH_RESULTS` stays off until search-engine eligibility is confirmed |
| 26 Service alerts — ALRT 001–007 (SRS 1.2) | done (policy pending D10) | `apps/api/src/website/{alert-rules,alert.service,alert.controller}.ts`, migration `20260907162953_website_service_alerts`, web `components/service-alert-{bar,banner}.tsx` above the header, admin `pages/website/ServiceAlertsPage.tsx` | `alert-rules.spec.ts` 11, `service-alerts.integration-spec.ts` 11 (real MySQL incl. the urgent purge), web `service-alert-banner.test.tsx` 6, runtime placement check | client alert policy and severities (D10) |
| 26 Testimonials — TSTM 001–005 (SRS 1.5: consent recorded, not required) | done (content pending D10) | `apps/api/src/website/{testimonial.service,showcase.controller}.ts`, migration `20260907170938_website_testimonials_partners`, home-page band in `apps/web/src/app/page.tsx`, admin `pages/website/TestimonialsPage.tsx` | `showcase.integration-spec.ts` (11, shared with partners), admin `Showcase.test.tsx` 5 | consented quotes from the client (D10) |
| 26 Client/partner logos — PTNR 001–005 (SRS 1.5: permission recorded, not required) | done (content pending D10) | `apps/api/src/website/{partner.service,showcase.controller}.ts`, same migration, home-page logo band, admin `pages/website/PartnersPage.tsx` | `showcase.integration-spec.ts` (11), admin `Showcase.test.tsx` 5 | written authorisations and logo assets from the client (D10) |
| 2 Public routes — UX 002/003 rename (SRS 1.3) | done | `/business`, `/business/category/{slug}`, `/business/area/{slug}`, `/business/{slug}`; 301s in `apps/web/src/middleware.ts`; "Businesses" navigation, breadcrumbs and footer; admin group and permission module read "Business"; reserved business slugs in `apps/api/src/common/slug.ts` enforced in `directory.service.ts`; sitemap emits the new paths | `next build` route list, runtime 200/301 checks including query preservation, `slug.spec.ts`, web 80, admin 129, API 216, `seo.integration-spec.ts` | — |
| 11 Information pages — CFG 002 (SRS 1.6: About + three policy pages, no editable Contact) | done (policy copy pending D05) | `apps/api/src/settings/{static-pages.ts,static-pages.service.ts}` (fixed slug registry with a `template` per page, publication gate, revisions), admin `apps/admin/src/pages/website/{PagesPage,PageEditorPage,StaticPageEditor}.tsx` at `/website/pages`, public `apps/web/src/app/(pages)/[slug]/page.tsx` (policy template) | `apps/api/src/settings/static-pages.spec.ts`, `apps/api/test/static-pages.integration-spec.ts` (13 assertions incl. the withdrawn contact slug answering 404), `apps/admin/src/pages/website/PagesPage.test.tsx` (5) | Client-approved privacy, terms and review-guidelines copy (D05); reviewed drop of the now-unused `static_pages.contact_email` column |
| 26 About page — ABT 001–006 (SRS 1.6) | done (photography and copy approval pending) | `apps/web/src/app/about/page.tsx`, `apps/web/src/components/about/{about-hero,about-metrics,about-process}.tsx`, live counts `apps/api/src/site/site-metrics.service.ts` + `GET /site/metrics`, `aboutPageJsonLd` in `apps/web/src/lib/structured-data.ts`, sitemap `pages` section, seed `apps/api/src/cli/seed-pages.ts` (`pnpm --filter api pages:seed`) | `apps/web/src/app/about/about-page.test.tsx` (10: publication 404, draft noindex, live counts, counts unavailable, genuine zero, CTA only with a published address, no development address, ordered process, link targets), `apps/web/src/components/site-header.test.tsx` (3), `apps/api/test/seo.integration-spec.ts` (metrics + `pages` sitemap), runtime at 1440/1024/768/390/320 px with no horizontal overflow and a 380 px desktop hero | Client photography and approved wording; both replace the shipped baseline through the editor and the media library |
| 25 Queue monitor — QMON 001–005 (SRS 1.2) | done | `apps/api/src/queues/{queue-registry,queue-monitor.service,queue-monitor.controller}.ts`, admin `apps/admin/src/pages/system/QueueMonitorPage.tsx` at `/system/queues` | `queue-registry.spec.ts` (7), `apps/api/test/queue-monitor.integration-spec.ts` (9, real Redis), `QueueMonitorPage.test.tsx` (7) | Job-name registry must be extended whenever the worker gains a job; the allowlist withholds details until then, by design |
| 25 Scheduled tasks — TASK 001–006 (SRS 1.2) | done | registry `packages/domain/src/scheduled-tasks.ts`; API `apps/api/src/schedules/*`; worker `apps/worker/src/{scheduled-tasks,schedule-runner}.ts`; admin `apps/admin/src/pages/system/ScheduledTasksPage.tsx` at `/system/schedules`; migration `20260907205054_scheduled_tasks` | `apps/worker/src/scheduled-tasks.spec.ts` (10: unknown code refused, lock skip, overlap-safe, one-line failures, timeout), `apps/api/test/schedules.integration-spec.ts` (9: dispatch not inline, unregistered codes, required tasks undisablable, permission separation, bounded history), `ScheduledTasksPage.test.tsx` (5) | Multi-replica behaviour proven by the lock unit tests; a live two-worker soak is part of the pre-launch drill |
| 9 Admin usability — ADM 002 (record editing on routes) | done | shared `apps/admin/src/components/ui/RecordEditorPage.tsx` and `apps/admin/src/shared/useRecordEditor.ts`; editors at `/categories\|services\|areas/:id`, `/blog-categories\|blog-tags/:id`, `/redirects/new`, `/admins/new`, `/businesses/featured/new`, `/media/:id`, `/website/pages/:slug`, `/website/{faqs,service-alerts,testimonials,partners}/:id` | Admin suite 37 files / 175 tests, including "no dialog on the list" and "sends only the selected ids" assertions per screen | Moderation, enquiry and publish confirmations stay dialogs by requirement (ADM 002, REV 004, CMGR 003) |
| 10 Administrator invitation — ADM 003, RBAC 004 | done | `apps/admin/src/pages/admins/AdminInvitePage.tsx` (roles chosen from the live role list, at least one required) | `AdministratorsPage.test.tsx` (3), including that nothing is sent until a role is chosen | Previously every invitation silently granted `super_admin`; fixed in Phase 40c |
| 11 Media retention — MED 004 | done | `media.retention` scheduled task in `apps/worker/src/scheduled-tasks.ts`; windows in `packages/domain/src/scheduled-tasks.ts` | `apps/worker/src/scheduled-tasks.spec.ts` (bytes deleted before rows; reports rather than deleting when storage is absent), `schedules.integration-spec.ts` (every stated retention policy has a task) | Was previously implemented in the API and called by nothing; corrected in Phase 41 |
| 11 Custom information pages — CFG 002 (SRS 1.7) | done | `apps/api/src/settings/static-pages.ts` (`pageSlugProblem`, `RESERVED_SLUGS`, system vs custom), `static-pages.service.ts` (`create`, `remove`), `POST`/`DELETE /admin/pages`; admin `PageCreatePage.tsx` and the delete action on `PagesPage.tsx`; public `app/(pages)/[slug]` now asks the API rather than holding a list | `static-pages.spec.ts` (9, incl. traversal and route-shadowing attempts), `static-pages.integration-spec.ts` (10, incl. both deletion refusals and the full create→publish→serve→delete cycle), `PagesPage.test.tsx` (10), `static-page.test.tsx` (web, 3) | Address is fixed at creation by design; renaming is a new page plus a redirect (SEO 004) |
| 25 Scheduled tasks — TASK 001–006 (audit fix F-01) | done | `scheduledTaskJobId()` in `packages/domain/src/scheduled-tasks.ts`; call sites in `apps/worker/src/schedule-runner.ts` and `apps/api/src/schedules/schedules.service.ts` | `packages/domain/src/scheduled-tasks.spec.ts` (5), a real-queue dispatch case in `apps/api/test/schedules.integration-spec.ts`, and a worker started and observed running the schedule | Before this fix the worker exited at start-up on every deployment; no scheduled task, enquiry delivery or media job could run |
| 8 Enquiry delivery — ENQ 003/005, MAIL 002 (audit fix F-02) | done (credentials pending D09) | `apps/worker/src/mailer/resend-mailer.ts`, transport selection in `apps/worker/src/{config,main}.ts` | `apps/worker/src/config.spec.ts` (Resend accepted, credentials validated, key never echoed); worker started under `MAIL_TRANSPORT=resend` | The API accepted the provider and the worker refused it, so enquiries could not have been delivered in the intended production configuration |
| 5 Public write protection — SEC 002/003 (audit fix F-03) | done (site keys pending D03) | `apps/api/src/common/captcha/turnstile.verifier.ts` | `turnstile.verifier.spec.ts` (7): unconfigured refuses, wrong hostname or action rejected, provider failure fails closed, secret never in the error | The control had no test of its own before this audit |
| 2 Accessibility — NFR 006 (audit fix F-04) | done (manual screen-reader pass outstanding) | `packages/ui/src/styles.css` `--ms-glass-dark` | axe clean at desktop and 320 px across the public routes; `apps/web/src/lib/palette.test.ts` now composites translucent panels over the surfaces behind them | A translucent panel measured 4.02:1 on the light bands; the previous test compared opaque tokens and passed |

| 20 Monitoring and observability — MON 001–002 (post-audit remediation, staging closure 2026-09-09) | done in the product; alert routing outstanding | `apps/api/src/observability/{metrics.registry,metrics.endpoint,metrics.collector,http-metrics.interceptor,worker-liveness.service}.ts`, `apps/worker/src/{observability,log,heartbeat}.ts`, `packages/domain/src/worker-heartbeat.ts`, `GET /api/v1/admin/system/queues/workers`, Workers card on `QueueMonitorPage.tsx`, `infrastructure/monitoring/*`, `docs/operations/{monitoring,alert-response,failure-drills}.md` | `metrics.endpoint.spec.ts` (4, incl. 404 for an unauthorised caller and no address/token/secret in any sample), `QueueMonitorPage.test.tsx` (9, incl. queue-reachable vs worker-running), `scheduled-tasks.spec.ts` cadence cases; verified live — worker heartbeat published, API `/metrics` reported `ms_worker_heartbeats 1` with real queue depths, `0` after the worker stopped | Verified 2026-09-09 with Prometheus in its own container scraping api + two worker replicas over a bearer token (`infrastructure/monitoring/`), all nine failure drills executed (`docs/operations/failure-drills.md`), and alert expressions checked against the recorded drill series. Outstanding: a paging destination and the on-call responder (client decision D07); object-storage, backup-success and restore-drill-age series are not emitted yet |
| 20 Release gate — OPS 002 (audit fix F-09) | done | `scripts/test-browser.mjs`, root `test:browser` and `verify:release` scripts, `browser` job in `.github/workflows/ci.yml` (prepared, not connected) | The runner fails on any skipped or missing test, provisions a throwaway administrator with a generated password, and drops its own database on success and on failure | `pnpm check` deliberately still does not start infrastructure |
| 25 Queue job identity — QMON 001, TASK 003 (audit fix F-01, hardened) | done | `queueJobId`/`assertQueueJobId`/`jobIdProblem` in `packages/domain/src/queue.ts`; gate in `apps/api/src/outbox/bullmq.queue.ts`; `scheduledTaskJobId` delegates to it | `packages/domain/src/queue.spec.ts` (8), `apps/api/test/queue-dispatch.integration-spec.ts` (6, real BullMQ: every task and job name, `:` refused, repeat dispatch idempotent, legacy id reachable, retry preserves id and payload) | Job-id construction is now one function; API and worker cannot diverge |
| 8 Mail provider configuration — ENQ 005 (audit fix F-02, hardened) | done | `packages/mail/src/transports.ts` (`MAIL_TRANSPORTS`, `PRODUCTION_MAIL_TRANSPORTS`), consumed by `apps/api/src/config/env.validation.ts` and `apps/worker/src/config.ts` | `transports.spec.ts` (4) plus agreement cases on both sides iterating the shared list | One list, two readers; drift is a test failure rather than a start-up failure in production |
| 8 Provider error redaction — SEC 004, MAIL 006 (new defect found during remediation) | done | `redactCredentials`/`redactSensitive` in `packages/mail/src/errors.ts`, used by both transports | `packages/mail/src/resend.spec.ts` credential-redaction cases | A provider echoing the request would have put the API key into logs, admin screens and stored failure reasons |
| 20 Public 404 document — SEO 004, NFR 006 (audit F-05, closed 2026-09-09) | done for the reference edge | `infrastructure/edge/nginx.conf`, `infrastructure/edge/docker-compose.edge.yml` — the proxy answers a 404 from the web upstream with Next.js's own prerendered `_not-found.html`, status preserved | Measured through real nginx in front of a production build: unknown business, article, category, locality and root routes return 404 with a 55,873-byte branded page (1,042 characters of visible text, `noindex`, no JavaScript, no redirect); `/api/v1/*` JSON envelopes, `/admin/` and `/_next/` untouched | Adapting the two directives to the chosen production edge is a staging task; the mounted not-found document is a build artefact and must be re-mounted on each deploy |
| Admin interface redesign — UX 001, NFR 006/011, RBAC 010 (10–12 September 2026) | done; two named previews outstanding | `apps/admin/src/config/theme.ts` (tokens, placeholder contrast), `apps/admin/src/components/ui/*` (incl. `RecordEditorPage` with an unsaved-changes guard and a side column that stacks below `lg`, `expandToggle`), `apps/admin/src/components/{FormSelect,FocalPointPicker}.tsx`, `apps/admin/src/styles/global.css` (preset tag contrast, `ms-field-row`, `ms-editor-grid`), `docs/design/admin-ui.md`, `docs/audits/admin-ui-inventory.md` | `e2e/scripts/route-sweep.ts` over all 72 routes at 1440 and 320 px (axe, overflow, console, h1/main, skip link, moderator permissions): 0 findings; `e2e/specs/admin-ui.spec.ts` (6 tests × 2 projects, now incl. media, redirects, the alert editor and the access editor's keyboard path); admin suite 42 files / 220 tests | Featured-placement preview and home-hero preview not built (inventory marks both `partial`); a manual screen-reader pass is still outstanding |
| Worker supervision and honest media state — MON 001–002, MED 002 (11 September 2026) | done in the product; supervision is a deployment task | `apps/worker/README.md` (required process, restart policy, liveness, graceful shutdown), `docs/operations/runbook.md` ("The worker"), `docs/operations/alert-response.md` (C4, W5); admin `MediaLibraryPage.tsx` reads `GET /admin/system/queues/workers` and states definitively when processing has stopped | `MediaLibraryPage.test.tsx`; live: two uploads stuck in `processing` with no worker, processed to `ready` once the worker ran | The worker must run as its own supervised process in every environment; nothing in the application can start it |
| Redirects: temporary kind and on/off state — SEO 004 (client instruction, 10 September 2026) | done | migration `20260910160747_redirect_temporary_and_active` (enum member appended last, `isActive` default true); `apps/api/src/seo/{redirect-rules,redirects.service,seo.controller}.ts` (`redirectEffect` shared by the public resolver and `GET /admin/redirects/resolve`; audited activate/deactivate; cache tag emitted and registered); `apps/web/src/middleware.ts` (resolved status, `no-store` on 302, 10 s cache); admin `RedirectsPage`, `RedirectEditorPage`, `RedirectPreviewPanel` | `redirect-rules.spec.ts`, `seo.integration-spec.ts` (14), `Redirects.test.tsx` | Slug changes still create 301s as SEO 004 requires; 302 is only for a redirect an administrator creates. An inactive rule answers 404 publicly, indistinguishable from no rule |
| Featured placement overlap — DIR 007 (two defects, 10 and 12 September 2026) | done | `apps/api/src/directory/featured.service.ts` (`assertNoOverlap`, shared by create and update, runs inside the write transaction behind a lock on the listing's row) | `featured.integration-spec.ts` (8, real MySQL: create, update, open-ended windows in both directions, six simultaneous overlapping creates → exactly one stored, an edit racing a create → one wins, unpublished and expired placements not shown, publish permission required) | `PATCH` skipped the check `POST` enforced; and the check was read-then-write, so simultaneous requests could all pass it — proven by removing the lock, when all six were accepted |
| Media "in use" — MED 004, DAT 003 (defect, 12 September 2026) | done | `packages/domain/src/media-usage.ts` (one list of relations plus the settings documents that name images by id), used by `apps/api/src/media/media.service.ts` (usages, deletion refusal, `unused` filter) and `apps/worker/src/scheduled-tasks.ts` (`media.retention`) | `media-usage.spec.ts` (3), `media.integration-spec.ts` (7, incl. testimonial, partner and site-logo images refused deletion and excluded from `unused`), `scheduled-tasks.spec.ts` (retention query excludes all of them) | Before the fix, testimonial portraits, partner logos, the site logo/icon/sharing image and home hero slides counted as unused: deletable from the library, and deleted by the worker 30 days after upload |
| Service alert presentation — ALRT 003–005 (10–12 September 2026) | done | `packages/domain/src/alerts.ts` (severities, tone, colours, role/aria-live, contrast; subpath `@melbourne-sphere/domain/alerts`), `packages/domain/src/urls.ts` (`validateAlertLink`, `validatePublicUrl`, now shared); API `alert-rules.ts`/`alert.service.ts` read both; web `service-alert-banner.tsx` renders from the table; admin `ServiceAlertPreview.tsx` and `ServiceAlertEditorPage.tsx` (Melbourne-time scheduling) | `alerts.spec.ts` (4, incl. AA contrast per band), web `service-alert-banner.test.tsx` (9, incl. 3 parity cases), admin `ServiceAlertPreview.test.tsx` (7, mirror parity) | The editor previously converted the schedule from the browser's timezone, not Melbourne's; the service decided link externality with its own heuristic |
| Administrator access editor — RBAC 010/011 (11 September 2026) | done | `apps/admin/src/pages/access/{AdminAccessCard,PermissionMatrix}.tsx`, `pages/admins/{AdminDetailPage,AdministratorsPage,AdminInvitePage}.tsx`, `src/shared/forensics.ts` | `AdminAccessCard.test.tsx` (6: sources by role name, redundant grant flagged, change named with sign-out before the write, self-edit absent, ungrantable permission withheld, Super Admin role withheld); `admin-ui.spec.ts` keyboard path | The server remains the authority (`assertNotSelf`, `assertMayAssignRoles`, last Super Admin); the screen withholds what it would refuse. A role that carries a permission the actor lacks is still refused only by the server |
| Password reuse — SECS 003 (client instruction, 12 September 2026) | done | `apps/api/src/auth/password-history.service.ts` (the current password is always refused; the configured depth adds earlier ones), `apps/api/src/settings/registry.ts` (`passwordHistoryDepth` default 3, relabelled) | `security-settings.integration-spec.ts` (default 3; reuse within depth refused; depth 0 still refuses the current password), `auth.integration-spec.ts` (a reset to the current password is refused and the link stays usable) | SECS 003 names history depth without a default; 3 is the client's choice |
| Sign-in and link screens — AUTH 001–003, NFR 006 (12 September 2026) | done | `apps/admin/src/layouts/AuthScreen.tsx` + `auth-screen.css`, `auth/sign-in-failure.ts`, `auth/sign-in-notice.ts`, `components/NewPasswordForm.tsx`, `pages/{LoginPage,ResetPasswordPage,AcceptSetupPage}.tsx`, `app/AppProviders.tsx` (Refine's login toast suppressed) | `LoginPage.test.tsx` (6), `sign-in-failure.test.ts` (4), `NewPassword.test.tsx` (5), `LoginTotp.test.tsx` (the challenge never reaches the screen), `auth-provider.test.ts` | Refine's own login toast had printed the two-step challenge token; removed |
| Admin themes — UX 001, NFR 006 (12 September 2026) | done | `apps/admin/src/config/theme.ts` (light/dark palettes, CSS variables, Ant theme), `theme/theme-mode.ts`, `components/ThemeToggle.tsx`, `styles/global.css` | `theme.test.ts` (AA contrast for both palettes), `ThemeToggle.test.tsx`, `admin-ui.spec.ts` (axe over every page family in dark; toggle survives a reload) | Light is the default; the choice is per browser |
| Transactional email presentation — ENQ 005, MAIL 001 (12 September 2026) | done | `packages/domain/src/email-layout.ts` (`renderEmail`, `escapeHtml`), `packages/domain/src/enquiry-mail.ts`, `apps/api/src/auth/mailer/auth-mail.ts`, HTML part through `packages/mail/src/{transport,resend-transport}.ts` and the API/worker mailers | `email-layout.spec.ts` (6: parts agree, escaping, http(s)-only links, solid colour under every gradient, visitor message escaped with line breaks), `auth-mail.spec.ts` (3: real link lifetime, one invitation builder), `transport.spec.ts` (HTML beside text, size bound), `enquiry-delivery.spec.ts` | Plain text is always sent; HTML is the alternative. Rendering checked in a browser at 720 and 390 px, light and dark |
