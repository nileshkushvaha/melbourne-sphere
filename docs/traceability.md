# Melbourne Sphere — SRS traceability

Maps SRS sections/requirements to implementation status, modules and evidence. Updated at the end of every phase. Status values: **done**, **partial**, **planned**, **blocked (decision)**, **future** (explicitly out of MVP).

| SRS section / IDs | Status | Modules / files | Evidence (tests, checks, runtime) | Remaining work |
| --- | --- | --- | --- | --- |
| 1 Scope — SCP 001–005 | partial | server-owned Melbourne constants (`apps/api/src/site/site.controller.ts`); local-area allowlist with council-area baseline seed (`taxonomy/seed-data.ts`); no city/tenancy code | Phase 11 integration tests (no expansion fields accepted); Phase 12 eligibility gate (`eligibilityVerifiedAt` required to publish, area must be active) | D01 boundary decision; QA T01 |
| 2 Public experience — UX 001–003 | partial | `packages/ui/src/styles.css` (light-first token system: colours, type, content widths, spacing, radii, shadows, focus rings, motion), `apps/web/src/components/page-shell.tsx` (full-bleed `Band` + bounded `.ms-container`), redesigned shell (`site-header.tsx` with mobile `<details>` navigation and the listing action, `site-footer.tsx`), redesigned home (`app/page.tsx`), cards (`business-card.tsx`, `post-card.tsx`, `featured-post-card.tsx`, `category-icon.tsx`); admin theme tokens (`apps/admin/src/config/theme.ts`) | `palette.test.ts` (AA contrast for every light surface and dark band, and a guard against reintroducing an OS dark scheme), `site.test.ts`, e2e `accessibility.spec.ts` (axe WCAG 2.2 AA scan of `/`, `/directory`, `/blog` and a listing at desktop and 320 px; mobile menu), browser review at 1440/1024/768/390 px | About appears in the navigation once that information page is published (Contact always resolves); brand logotype and client photography |
| 3 Architecture — ARC 001–005 | partial | pnpm workspace; `apps/web`, `apps/api`, `apps/admin`, `apps/worker`, `packages/{database,contracts,ui,domain}`; same-origin `/api/v1` proxies; Refine vertical slice | `pnpm check` (all workspaces build), bundle scans (no Refine/antd in public chunks), worker runtime check | `packages/config`; S3 storage adapter (phase 20) |
| 4 Hero — HERO 001–007 | done (interim photography) | `apps/web/src/components/hero-banner.tsx` (full-bleed photograph, directional wash on `sm`+ and an even tint below it, per-slide focal points, cross-fade with previous/next/pause/per-image controls, designed gradient fallback), `hero-headline.tsx` (display-serif H1, reserved-height rotating phrase), `hero-search.tsx` (rounded panel: fixed Melbourne label, labelled keyword combobox, category select, prominent Search), `lib/hero-assets.ts` (licensed default slides), `apps/api/src/settings/*` | `hero.test.ts`, `hero-banner.test.tsx` (axe, controls, decorative images), `settings-suggestions.integration-spec.ts`, `home-settings.spec.ts`, e2e keyboard search + axe, browser review at four widths | Client's own photography replaces `DEFAULT_HERO_SLIDES` (see `docs/content/hero-photography.md`); counters stay hidden until an admin enables them |
| 5 Discovery — DIR 001–007 | partial | `apps/api/src/directory/search/*` (SearchService, ranking, facets), `dto/public-business.dto.ts`, `directory-public.controller.ts`; web `directory-results.tsx`, `search-params.ts`, filters/chips/pagination, listing cards with a category-derived branded fallback instead of one shared placeholder (`business-card.tsx`, `lib/category-visuals.ts`); DIR 007 featured block (`featured.service.ts`, at most three matching published listings in `meta.featured`, excluded from organic results and pagination, labelled block on the web) | `search.integration-spec.ts`, `search-rules.spec.ts`, `search-params.test.ts`, `featured.integration-spec.ts`, `category-visuals.test.ts`, runtime checks | DIR 008 "Open now" (deferred, needs hours coverage); suggestions delivered in phase 15 (`suggestions.service.ts`) |
| 6 Listings — BUS 001–008 | partial | BUS 002/006/007 core in `apps/api/src/directory/` (rules, service, admin controller), schema `businesses*` tables; BUS 003 phone/link rules (`business-rules.ts`); BUS 004 hours (`directory/hours/*`, `opening_intervals`, `hours_exceptions`); BUS 008 local areas (`taxonomy.service.ts`); admin `pages/businesses/*` incl. `HoursEditor` | Phase 12: `directory.integration-spec.ts`, `business-rules.spec.ts`, admin `BusinessesPage.test.tsx`; Phase 13: `melbourne-time.spec.ts`, `hours-rules.spec.ts` (DST), `hours.integration-spec.ts`, runtime curl + browser | gallery (phase 20); BUS 001 public presentation, BUS 003 contact actions and BUS 005 related listings delivered in phase 14 (`apps/web/src/app/business/[slug]/page.tsx`, `hours-table.tsx`) |
| 7 Reviews/reports — REV 001–005, REP 001–002 | done (reports cover comments too) | `apps/api/src/reviews/*` (submission guards, moderation, aggregates, reports), `common/captcha/*`, `common/idempotency.service.ts`, `reviews`/`abuse_reports`/`idempotency_records`; admin `pages/moderation/*`; web `review-form.tsx`, `review-list.tsx`, `rating-panel.tsx`/`rating-stars.tsx` (average, partial-fill stars and the approved-review distribution `ratingBreakdown`, computed over every approved review rather than the page on screen) | `reviews.integration-spec.ts` (16 assertions groups), `review-rules.spec.ts`, admin `Moderation.test.tsx`, web `submissions.test.ts` and `rating-panel.test.tsx` (axe), `reviews.integration-spec.ts` distribution case, runtime fail-safe check | retention purges (phase 22); real Turnstile keys (D03) |
| 8 Enquiries — ENQ 001–007 | partial | `apps/api/src/enquiries/*`, `outbox/*`, `apps/worker/*`, `packages/domain/*`, tables `enquiries`/`outbox_events`/`provider_message_events`; admin `pages/enquiries/*`; web `enquiry-form.tsx` and `contact-form.tsx` (the `/contact` page posts the general enquiry through the same pipeline, arriving in the queue as `kind: site`) | `enquiries.integration-spec.ts`, `enquiry-delivery.spec.ts`, `enquiry-mail.spec.ts`, worker `config.spec.ts`, admin `EnquiriesPage.test.tsx`, runtime end-to-end delivery | ENQ 006 webhook ingestion and bounce handling (needs provider, D03); provider adapter; retention purge (phase 22) |
| 9 Blog — BLOG 001–005, COM 001–002 | done | `apps/api/src/blog/*` (sanitiser, editorial service, public reads, comments), tables `posts`/`authors`/`blog_categories`/`blog_tags`/`post_tags`/`content_revisions`/`comments`; admin `pages/blog/*` and `pages/moderation/CommentsPage`; web `app/blog/**`; full author profiles (`authors` + `author_links`, `blog/author-rules.ts`), rich-text article bodies (`bodyFormat`, TipTap editor in `admin/src/components/RichTextEditor.tsx`) sanitised by the same allowlist, article cover media, public byline and author card (`web/src/components/author-byline.tsx`, `author-card.tsx`) | `blog.integration-spec.ts`, `blog-public.integration-spec.ts`, `sanitise.spec.ts`, `post-rules.spec.ts`, `share.test.ts`, admin `Blog.test.tsx`/`CommentsPage.test.tsx`, runtime route and header checks, `authors.integration-spec.ts`, `author-rules.spec.ts`, admin `Authors.test.tsx`, e2e `editorial.spec.ts` + axe scan of `/blog`, browser review of the index and article at desktop and 390 px | cover images (phase 20); sitemap and JSON-LD (phase 21); cache purge (phase 22) |
| 10 Admin/auth — ADM 001–003, AUTH 001–003, RBAC 001 | partial | as before, plus ADM 003 dashboard aggregates scoped to the caller's permissions with no private text (`apps/api/src/dashboard/*`, `admin/src/pages/DashboardPage.tsx`), RBAC 001 extended with `redirects.manage`, and the ADM 002 screen set now covering listings, taxonomy, media, articles, authors, editorial taxonomy, enquiries, moderation queues, settings, SEO redirects, administrators, account security and audit log on one design system (grouped navigation, `PageHeader`/`SectionCard`/`StatCard`/`StatusTag`/`EmptyState`/sticky save bar) | API integration 101 + e2e 19 + unit 141; admin 92 (incl. `Dashboard.test.tsx`, `Authors.test.tsx`, `Redirects.test.tsx`); `dashboard.integration-spec.ts` permission scoping | remaining screen-by-screen polish (phase 22), mandatory-TOTP policy (D06), audited second-factor reset action |
| 16 Site configuration — CFG 001–002 | done | home/hero settings incl. the banner slides and counters (`apps/api/src/settings/*`), the five information pages with sanitised rich content, revisions and a publication gate refusing stub or placeholder copy and unvalidated contact routing; admin screens for both; public `/{slug}` pages on a shared `InformationPage` layout and footer links only when published; `/contact` always resolves — published copy when it exists, otherwise a factual explanation of how to reach the editors, `noindex` until the approved copy is published | `settings-suggestions.integration-spec.ts`, `static-pages.integration-spec.ts`, `static-pages.spec.ts`, admin `SiteSettingsPage.test.tsx` | logo, social URLs and default share image await client assets; the approved public contact address is still outstanding, so `contactChannel()` withholds development addresses and the listing action points at the homepage explanation instead (`apps/web/src/lib/site.ts`, `site.test.ts`) |
| 12 SEO — SEO 001–007 | mostly complete | server-rendered pages with one H1, title template, absolute canonical (`SITE_ORIGIN`), tracking parameters stripped, `noindex, follow` on filtered searches, crawlable prev/next; `robots.txt`, `/sitemap.xml` index with per-section children (`apps/web/src/app/sitemap.xml`, `sitemaps/[section]`), JSON-LD builders with script-safe escaping (`apps/web/src/lib/structured-data.ts`), redirect table and `middleware.ts` (301/410, no chains or cycles) | `seo.integration-spec.ts`, `structured-data.test.ts`, `pnpm check` | review rich results held for the technical lead (SEO 006); cache purge on publish (phase 22); Search Console (phase 24) |
| 13 Data model — DAT 001–003 | partial | Prisma 7 foundation, `SystemProbe`, utf8mb4 + `utf8mb4_unicode_ci` policy (migration `collation_policy_*`, `check-migrations.mjs`) | Phase 6/8 integration tests; `pnpm db:migrations:check` | product entities |
| 14 Editorial/operational model — DAT 004–006 | partial | Post (with `bodyFormat`, `coverMediaId`), full Author profile plus `author_links`, BlogCategory, Tag, ContentRevision, Comment, MediaAsset/MediaVariant, `redirects`, outbox and idempotency records; versioned migrations and the policy lint | `blog.integration-spec.ts` (unique slugs, transactional publication, revisions), migration checks | StaticPage and SeoMetadata (phase 22) |
| 15 REST contract — API 001–004 | partial | as before plus `GET/POST /businesses/{id}/reviews`, `POST /reports`, `GET /home`, `GET /search/suggestions`; API 003 idempotency (`idempotency_records`, `Idempotency-Key`, replay receipts) | API e2e (19), `search.integration-spec.ts`, `reviews.integration-spec.ts`, `pnpm check` | enquiry, blog and static-page routes (phases 17–19, 22) |
| 16 Admin API — API 005, MOD 001–002 | partial | `/api/v1/admin/auth/*` (AuthenticationModule), IdentityModule, AuditModule; default-deny guard chain; readiness for MySQL + Redis | integration tests incl. undeclared-route denial | remaining module families (phases 10–20), expectedVersion on updates |
| 17 Events/caching — EVT 001–002, CACHE 001–003 | done | transactional outbox and dispatcher; BullMQ worker with five attempts and backoff; Redis read cache namespaced by publication version (`apps/api/src/cache/*`), `cache.invalidate` events written in the same transaction as every publication, featuring, moderation, page or settings change, worker purge to the web tier (`apps/web/src/app/api/revalidate`) with retries; explicit Next.js revalidate/tags per fetch | `cache-invalidation.integration-spec.ts`, `cache-invalidation.spec.ts` (worker), `queue.spec.ts`, `enquiries.integration-spec.ts` | CDN purge hooks once a CDN exists (phase 24 client decision) |
| 18 Security/privacy — SEC 001–004, PRIV 001–002 | partial | allowlist sanitisation of all editorial content (`blog/sanitise.ts`) protecting public pages and admin previews; server-side Turnstile (`common/captcha/*`), honeypots, public per-IP ceilings (`public-rate-limit.service.ts`), keyed email/IP hashes, encrypted reporter and reviewer emails, acknowledgement version stored | api unit/e2e/integration tests incl. fail-safe 503 paths; scans each phase | retention jobs (phase 22+), TLS/CSP on web tier (phases 23–24), dependency/container scanning gates (phase 24) |
| 19 NFR 001–013 | mostly complete | axe-core checks on admin screens and the public hero, keyboard/landmark/320 px journeys in Playwright, heading and table-header defects fixed, reduced motion with pause controls, code splitting with an enforced bundle budget, NFR 003 capacity profile passing at 10k businesses / 2k articles / 100k reviews (`tools/load`) after fixing a fixed 10-connection pool and a cache stampede | `pnpm check`, `e2e/specs/accessibility.spec.ts`, `tools/load/run-load.mjs` | manual screen-reader pass; POST-path load measurement on staging |
| 20 Deployment/ops — OPS 001–004, BACK 001–002, MON 001–002 | partial | CI workflow (frozen lockfile, checks, integration services, migrations once, dependency audit), non-root multi-stage Dockerfiles for api/worker/web, `GET /admin/operations/status` signals with MON 002 thresholds, encrypted backup and isolated restore-drill scripts, `docs/operations/runbook.md` and drill log | `operations.integration-spec.ts`; restore drill executed and recorded in `docs/operations/restore-drills.md`; CI runs on the client's infrastructure | environments, region, CDN, error tracking, uptime checks, on-call responder and the first restore drill are client actions |
| 21 Verification — QA 001–003 | mostly complete | Vitest unit/e2e; integration harness against the isolated `_test` MySQL/Redis (API 115, database 5); accessibility specs; Playwright UAT journeys for all four QA 003 flows (`e2e/`); `docs/pre-audit-report.md` | `pnpm check`, `pnpm test:integration`, `pnpm --filter @melbourne-sphere/e2e test` | run the journeys against staging and assemble the signed evidence pack (client) |
| 22 Sequence | n/a | this roadmap | `docs/setup-progress.md` | — |
| 23 Decisions D01–D08 | blocked (decision) | baselines applied: council-area boundary, TOTP optional | — | client decisions before launch |
| 24 Audit handover | partial | `docs/pre-audit-report.md` (internal pre-audit: verification summary, requirement coverage, security review, open items) | `pnpm check`, `pnpm test:integration` | external audit, UAT evidence pack and signed launch decisions (client) |
