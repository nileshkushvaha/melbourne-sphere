# Current state (AI handoff) — updated 2026-09-07 (SRS 1.3; System & Settings / Security / Website extension underway at Phase 30 of 41)

Overwrite this file at every phase gate; keep it factual and short. History lives in `docs/setup-progress.md` (and `docs/history/`), requirement status in `docs/requirements-traceability.md`.

## Phase
- **Active:** the SRS 1.2/1.3 extension. Phases 1–29 (the MVP) are complete. The client authorised a new programme on 2026-09-07: System & Settings (email logs, activity log, cache manager, queue monitor, scheduled tasks), Security Settings (authentication, password policy, login security, session security) and Website content (FAQs, service alerts above the public header, testimonials, client/partner logos), with **Resend** as the planned production email provider and Mailpit kept for local development. The roadmap is **Phases 30–41** in `docs/setup-progress.md`; implement one module at a time and run the gate before the next.
- **Last completed:** **Phase 42** — administrators can add their own pages (SRS **1.7**). The closed slug set is replaced by validation: a strict pattern, a reserved list covering every public route plus the framework and admin paths, and a uniqueness check — so a created page can never shadow `/blog` or `/contact`. System pages (About + the three policies) stay code-declared and undeletable; custom pages are `static_pages` rows the registry does not name, so no migration was needed. Addresses are fixed at creation; deletion is refused for system pages and for anything still published. `app/(pages)/[slug]` now asks the API instead of holding its own slug list. Before it: **Phase 41** — the cross-module audit. One real finding: the MED 004 media-retention policy existed in `MediaService.runRetention` and was called by nothing, so it had never run; it is now the `media.retention` scheduled task in the worker (where the object storage is), the API's uncalled copy is gone, and the schedules integration test asserts that every retention policy the SRS states has a task applying it. One observation left alone: the protected role still carries the retired `system.activity_logs.view` permission, which grants nothing — untidy, not unsafe, and a job for the next reviewed migration. Before it: **Phase 40c** — every record editor now lives on its own route (taxonomy, editorial terms, redirects, administrators, featured, media, pages, website content), the fourteen copies of the submit/error path collapsed into `shared/useRecordEditor.ts`, and `PageHeader` is used by every screen. Two defects surfaced on the way: administrator invitations were sending `roleKeys: ['super_admin']` for everyone (roles are now chosen, at least one required), and two editors had `if (fields)` on an always-truthy object. Admin suite capped at four workers so a full run is not decided by machine load. Before it: **Phase 36** — scheduled tasks (TASK 001–006): one registry in `packages/domain/src/scheduled-tasks.ts` shared by API and worker; the worker owns the schedules and implementations, the API shows state and dispatches a *queue job* (never an inline run); a Redis lock per task, timeouts, one-line failure records, 30-day history; publication and the three retention tasks refuse to be switched off. Migration `20260907205054_scheduled_tasks`. Before it: **Phase 35** — queue monitor (QMON 001–005): allowlisted payload redaction, worker availability labelled an estimate, bounded per-item bulk actions, pause with its consequence stated, and no route that creates a job. Before them: **Phase 40b** (2026-09-08, two client instructions): the editable information-page set is now About + Privacy Policy + Terms of Use + Review Guidelines — the editable Contact page is withdrawn because `/contact` routes from the general settings (CFG 001) and a second editable address is a second answer — and the master-detail screen is replaced by **Website → Pages** (`/website/pages`, `/website/pages/:slug`), one list plus one shared `StaticPageEditor`. `/about` is a bespoke server-rendered template over the same CMS record (`template: 'about'`), composing the administrator's title, introduction and SEO fields with live counts from the new `GET /site/metrics`, the configured contact route and factual process copy; a count that cannot be taken is null and its band disappears rather than showing zeros. `pnpm --filter api pages:seed` writes and publishes shipped baseline About copy into an *empty* row only. SRS **1.6** records all of it (CFG 002 amended, SEO 002 amended, new ABT 001–006). Before it: **Phase 40a** (2026-09-08, two client instructions): record editing moved out of dialogs onto their own routes behind one shared `RecordEditorPage` layout (FAQs, service alerts, testimonials, partners done; taxonomy, editorial terms, redirects, administrators, featured, media and the moderation decision dialogs still to convert), and publication of testimonials and partner logos no longer requires a recorded approval or authorisation (SRS 1.5) — the records remain optional evidence, and a partner still needs a logo and alternative text because those are about the page rendering, not permission. Confirmation dialogs stay: ADM 002 and several 1.2 requirements name them. Before it: **Phase 34** — the cache manager (CMGR 001–005): two registries (Redis namespaces and web-tier tags), nothing reachable outside them, protected key spaces proven untouched, and a real ioredis `keyPrefix`/SCAN bug caught by the tests before it shipped. Before it: **Phase 33** — security settings (SECS 001–008): eight settings, each with its enforcement named and none able to weaken AUTH 001/002 or SEC 002; consequences applied immediately (sessions ended, password history pruned) and audited; session administration re-scoped to its own permissions. Mandatory two-factor is deliberately absent until D06. Before it: **Phases 39–40** — testimonials and client/partner logos, completing SRS section 26. Both refuse publication without recorded permission (approval for a quote, authorisation plus alt text for a logo), and both clear that permission when the thing it covered changes. Also **Phase 38a/38b**, two client requests: the hero pause controls moved out of the visual composition but kept in the accessibility tree (auto-motion retained), and the contact strip now scrolls away while the navigation stays pinned, with service alerts above it. Before them: **Phase 38** — service alerts above the public header (ALRT 001–007): severity-driven accessible semantics, deterministic selection, write-time link validation, version-aware dismissal and an urgent purge when a live alert is taken down. Before it: **Phase 37** — FAQs (FAQ 001–005) with a `<details>` public page and gated `FAQPage` structured data; **Phase 32** — the consolidated activity log (ACT 001–006): an activity catalogue giving every event a category, label and derived outcome, `/admin/audit` renamed to `/admin/activity` with category/outcome/request-id filters, a 365-day retention purge, and the admin screen relabelled Activity log. Before it: **Phase 31** — Resend provider, delivery records, signed provider webhook and the read-only admin email log (MAIL 001–010). Before it: **Phase 30** (settings architecture and permission catalogue, SET 001–005/RBAC 013) and **Phase 30a** (enquiry form moved into the listing sidebar; `/directory` renamed to `/business`, SRS 1.3). Settings storage was corrected at client instruction to one `settings` table keyed by (group, key) with one shared persistence path (SRS 1.4, SET 001).
- **Next:** the client's programme is delivered end to end (Phases 30–41). What remains is theirs, not ours: decisions **D01** (Greater Melbourne boundary), **D05** (approved privacy, terms and review-guidelines copy), **D06** (mandatory TOTP), **D09** (Resend account, domain, DNS and webhook secret) and **D10** (website content and rights), plus commissioned Melbourne photography to replace the interim licensed images on the home and About pages. A two-worker soak of the scheduled-task locks belongs in the pre-launch drill.
- **Servers:** the client runs their own API (3001) and admin (3002); a temporary production web preview on **3010** was used for Phase 40b verification and stopped afterwards. Never stop a server you did not start. The Docker infrastructure (MySQL 3307, Redis 6380, Mailpit 1025/8025, MinIO 9010/9011, Adminer 8082) is still running. Start with `pnpm dev:api` / `dev:admin` / `dev:web`; use ports 3011/3211 for any verification instance so the client's own servers are never disturbed. Order chosen for visible value first and to keep the highest-risk change (auth settings) away from the end of a long run.
- **SRS revision:** **1.7**. Sections 25 (operational administration: SET/MAIL/ACT/SECS/CMGR/QMON/TASK/RBAC 013) and 26 (website content: FAQ/ALRT/TSTM/PTNR) were added at 1.2; 1.3 renamed the public directory routes (UX 002/003). `docs/ai/srs-index.md` carries the current line map and MD5. The read-only Logimart reference that informed sections 25–26 is `docs/reference/logimart-comparison.md` — a behavioural reference only, with a register of security weaknesses and Laravel-specific patterns that must not be copied.
- **Client feedback in force:** the interface must read as a premium, industry-standard product. The home page has been fully recomposed (Phase 26) on a light-first token system with deliberate dark bands; the admin shell and editorial screens were reworked earlier. The home page, information pages, blog (index, article, category, tag), business list/category/area and listing detail have all been recomposed on the same system. Reuse `packages/ui/src/styles.css` tokens, `components/page-shell.tsx` (`Band`, `SectionHeading`, `gridColumns`), `components/collection-header.tsx`, `components/information-page.tsx` and the card patterns rather than inventing new ones.
- **Public theme decision:** the public site is light-first with designed dark bands and **no OS dark scheme**. `apps/web/src/lib/palette.test.ts` fails if a `prefers-color-scheme` block reappears in the public tokens; changing that is a deliberate design decision, not a token edit.

## Public routes (renamed at 1.3)
`/business` (list), `/business/category/{slug}`, `/business/area/{slug}`, `/business/{slug}` (detail), `/blog…`, `/{slug}` information pages, `/contact`. Every `/directory…` address answers **301** to its `/business…` equivalent from `apps/web/src/middleware.ts`, preserving the query string; this is a mechanical mapping, not a seeded redirect row. `category` and `area` are **reserved business slugs** (a static segment wins over the dynamic one). Navigation, breadcrumbs and the footer read "Businesses"; the admin navigation group and the permission-matrix module read "Business".

## Implementation status
- **apps/api (1.2 additions):** owned settings groups — `settings/registry.ts` declares the security, email, operations and website groups with per-setting metadata (type, bounds, default, visibility, runtime/restart, sensitivity, permissions, cache tags, `enforcedBy`); `settings-store.service.ts` implements the SET 003 change contract once; `settings-groups.controller.ts` serves `GET /admin/settings/registry` (session-only, filtered to what the caller may view) and a route pair per group. The security, email and operations groups deliberately declare **no settings yet** — each lands with its enforcement in its own phase, because SET 005/SECS 001 forbid a stored-but-unenforced control. Tables `security_settings`, `email_settings`, `operations_settings`.
- **apps/api:** operations signals (`/admin/operations/status`), read cache with publication-namespaced keys and transactional purge events, information pages, featured placements, health/readiness, config validation, site settings (`site_settings`, public `/home`), general settings (`settings/general-settings.ts`: identity, branding media, contact details, header bar, one social profile per platform incl. Pinterest, footer copyright template; public `/site/settings`), search suggestions with a public rate ceiling, reviews and abuse reports, enquiries with a transactional outbox and dispatcher, the blog (editorial core, public reads and comments) with Markdown sanitisation and scheduled publishing, the media pipeline (signed uploads, validation, usage records, retention), all public POSTs idempotent and rate-limited behind Turnstile and honeypots, auth (Argon2id, opaque cookie sessions, CSRF origin guard, Redis throttling, optional TOTP, reset/setup tokens), admins lifecycle, audit, taxonomy (categories 2-level, services+synonyms, local areas), directory (listings, publication gates, duplicates, hours, links, public search/detail, and the approved-review `ratingBreakdown` on listing detail), and the **AuthorizationModule** (effective-permission resolver `roles ∪ direct` with a Redis cache keyed by an authorization version, CASL 7.0.1 ability factory behind the default-deny guard, roles/permissions/administrator-access API with `expectedVersion`, transactional audit and the privileged invariants). OpenAPI at `/api/v1/openapi.json` when `OPENAPI_ENABLED=true`.
- **apps/admin:** permission-aware throughout (Refine `accessControlProvider` fed by the server's effective codes, one route→permission mapping, a route guard with a waiting state and an accessible forbidden page, roles list, role editor with a module-grouped permission matrix, read-only permission catalogue and an administrator access editor showing inherited vs direct grants and the source of each capability); grouped-navigation shell (Overview/Directory/Editorial/Community/Configuration) with an account menu, shared design-system primitives in `src/components/ui/*` (PageHeader, SectionCard, StatCard, StatusTag, EmptyState, StickyActions), a real dashboard (`GET /admin/dashboard`), login/TOTP, administrators, account security, audit log, taxonomy screens, Businesses list/editor (hours editor, links, gallery), Reviews, Comments, Abuse reports and Enquiries queues, Media library, Articles (TipTap rich-text editor, cover picker, SEO snippet, slug change with redirect), Authors (list + full profile editor), Blog categories/tags, Site settings, General settings (identity, branding, contact bar, social profiles, footer), SEO redirects, information pages and featured listings. One loading screen (`components/ui/PageLoader.tsx`) covers route code, the session check and record loads, a boot loader in `index.html` covers the first paint, and the app boundary tells a stale build apart from a genuine fault. Heavy screens and the editor are code-split (entry 755 kB; `pnpm budget` enforces the cap). axe-core checks run over representative screens.
- **apps/web:** public shell (full-width navy header with a native `<details>` mobile menu, structured dark footer, skip link), a fully recomposed home page (photographic hero → light categories → soft listings → dark Melbourne areas → light stories → dark CTA, each band loading independently with populated/empty/failure states), `/business` (route group `(list)`), `/business/category|area/[slug]`, `/business/[slug]`, blog (index, category, tag, article with byline, author card and comments), a designed status treatment for 404/500 (`status-page.tsx`, `error.tsx`, `global-error.tsx`, `global-not-found.tsx`, per-segment not-found), a client navigation indicator (`route-progress.tsx`), brand marks for every profile link (`brand-icon.tsx`) and star-icon ratings, hero with rotating phrases and suggestion combobox, approved reviews, review and enquiry forms; hero banner slider with admin-managed photography and licensed CC BY defaults (`lib/hero-assets.ts`, `public/hero/`, `docs/content/hero-photography.md`), category-derived branded card fallbacks (`lib/category-visuals.ts`), information pages at `/{slug}` and a `/contact` route that always resolves (published copy, else a factual explanation plus the contact form posting to `POST /api/v1/contact`), `robots.txt`, `/sitemap.xml` + `/sitemaps/[section]`, JSON-LD (`lib/structured-data.ts`), redirect `middleware.ts` (301/410), `POST /api/revalidate` purge endpoint, shared `.ms-prose` editorial styling; the shell (name, logo, contact bar, social profiles, footer copyright, metadata) comes from the general settings with shipped defaults if that read fails; server-only API client with explicit revalidate/tags. Policy pages (about, privacy, terms, review guidelines) await approved copy and stay unlinked until published.
- **apps/worker:** BullMQ consumer for `cache.invalidate` (web-tier purge with retries), `enquiry.email` (mailer port with `console` for development and `smtp` via `@melbourne-sphere/mail` for production; one attempt per job, queue retries, stable `Message-ID`, redacted errors) and `media.process` (sharp variants, EXIF stripped). The worker does not load a `.env` file: export the API's variables (plus `MEDIA_S3_*` from the MinIO credentials) before `node apps/worker/dist/main.js`.
- **packages/database:** 20 migrations (last `authorization_roles_and_direct_permissions`: role activation/version, permission label/module/state, `admin_permissions` direct grants, assignment provenance, `admin_users.authzVersion`, `audit_logs.userAgent`), applied to dev and test DBs. New since Phase 20: `redirects`, author profile columns + `author_links`, `posts.bodyFormat`. **packages/contracts:** generated from the API. **packages/ui:** tokens + primitives. **packages/domain:** enquiry mail composition and queue policy shared by the API and worker. **packages/mail:** the SMTP transport (`smtpConfigFromEnv`, `SmtpTransport`, `TransientMailError`/`PermanentMailError`, `redactAddresses`); build it with `pnpm mail:build` before typechecking the API or worker (`pnpm check`, CI and the Dockerfiles do).

## Versions (selected, pinned in lockfile)
Node 24.19.0 · pnpm 12.3.4 · @casl/ability 7.0.1 · Next 16.3.4 · React 19.2.8 · TypeScript 6.0.3 (api, admin, database) / 5.9.3 (contracts, ui) / ^5 (web, forced by eslint-config-next) · NestJS 12.0.1 · @nestjs/swagger 12.0.1 · Prisma 7.10.0 + @prisma/adapter-mariadb 7.10.0 · ioredis 5.11.1 · @node-rs/argon2 2.2.0 · otpauth 9.5.2 · Vitest 4.1.11 · Vite 8.2.2 · Refine core 5.0.12 / antd 6.0.3 / react-router 2.0.4 · antd 5.29.3 · react-router 7.18.3 · Tailwind 4.3.3 · openapi-typescript 7.13.0 · class-variance-authority 0.7.1 · tailwind-merge 3.6.0 · @radix-ui/react-slot 1.3.3 · lucide-react 1.41.0 · @icons-pack/react-simple-icons 13.15.1 · MySQL 8.4.11 · Redis 8.4.6 · Adminer 5.5.1 · nodemailer 10.0.0 · Mailpit v1.31.1.

## Dependency decisions (do not re-research unless adding/upgrading/broken)
| Dependency | Decision | Why / constraint | Reconsider when |
| --- | --- | --- | --- |
| Prisma 7.10 (not 8 RC) | hold on 7 | 8 is pre-release; adapter-mariadb pairs with 7 | Prisma 8 GA |
| ioredis 5 (not 6) | hold | BullMQ/Nest ecosystem peers target 5 | BullMQ declares ioredis 6 support |
| antd 5 + react-router 7 | required by Refine 5 peers | `@refinedev/antd` 6 peers antd 5; `@refinedev/react-router` 2 peers react-router 7 | Refine publishes antd 6 support |
| `@ant-design/v5-patch-for-react-19` | required | antd 5 + React 19 compatibility | antd 6 |
| ESLint 9 in apps/web (EOL) | blocked upstream | `eslint-config-next` 16 plugin peers do not accept ESLint 10 | eslint-config-next supports ESLint 10 |
| TypeScript 5.9 in contracts/ui | pinned | openapi-typescript 7 peer range; web build uses TS 5 | openapi-typescript supports TS 6 |
| `@scarf/scarf` build script | disabled in `pnpm-workspace.yaml` | install-time telemetry | never |
| Node Temporal | not available in Node 24 | hours use an Intl-based converter (`directory/hours/melbourne-time.ts`) | Temporal ships unflagged |
| Search | MySQL LIKE with rank CASE (no FULLTEXT) | DIR 003 allows indexed prefix + bounded fallback; ≤10k listings | search quality complaints or NFR 003 load results |
| sharp 0.35.4 + @aws-sdk/client-s3 3.1127 + file-type 22 | image processing and S3 | re-encoding strips EXIF; the S3 client works against MinIO and providers alike | provider chosen (D03) |
| MinIO RELEASE.2025-09-07 | local S3 on ports 9010/9011 | 9000 is taken by php-fpm on this machine | production uses the real provider |
| marked 18 + sanitize-html 2.17 | both authoring paths | SEC 001 requires an allowlist; Markdown (legacy) and editor HTML pass the same allowlist server-side | — |
| TipTap 3.31.3 (`@tiptap/react`, `pm`, `starter-kit`, `extension-image`, `extension-table`) | admin rich-text editor | ProseMirror-based, React 19 peer support; output is sanitised server-side, so the editor is never the security boundary; lazy-loaded into its own chunk | TipTap 4 |
| Instrument Serif (next/font) | display headings only | one static weight, self-hosted at build time alongside Geist; falls back to Georgia | a brand typeface is supplied |
| `@casl/ability` 7.0.1 | pinned, used directly | MIT, dual CJS/ESM with types, one dependency; NestJS documents the pattern but ships no package, and a wrapper would own the guard, principal and caching we already own (ADR 0001) | a row-level policy requirement appears (then evaluate `@casl/prisma`) |
| BullMQ 6.3.4 | current major | pairs with ioredis 5; queue policy lives in `packages/domain` | BullMQ 7 |
| Turnstile | one real verifier, no dev bypass | a bypass would void SEC 002/003; without the secret, public writes answer 503 | client supplies keys (D03) |
| nodemailer 10.0.0 (`packages/mail`) | SMTP is the email boundary | every D03 candidate (SES, Postmark, Mailgun, SendGrid, Resend, Brevo, host relay) offers authenticated SMTP, so the provider is a credential change; ships its own types, ESM+CJS, Node ≥ 20 | provider chosen and its HTTP API/webhooks (ENQ 006) are needed |
| Mailpit v1.31.1 | local SMTP catcher on 1025/8025 | same adapter locally as in production; production rejects loopback/plaintext/unauthenticated relays at start-up | — |

## Ports and service ownership
- 3000 web (`next start`, user-started, previous build) and 3100 (`next start`, user-started): leave running. 1025/8025 Mailpit (Compose, running; started this session as part of the stack). Interactive web checks must use `next build && next start` (dev-mode hydration is blocked in the sandboxed browser pane because its HMR socket cannot connect). 3001 API and 3002 admin: **dev servers started for the client's own browser testing — leave them running unless asked**. 3307 MySQL, 6380 Redis, 8082 Adminer, 9010/9011 MinIO, 1025/8025 Mailpit (Compose, running). 3306 Homebrew MySQL: off-limits.
- After a schema or permission change, run `pnpm --filter api admin:seed-rbac` so new permission rows exist (it is idempotent); the 41 codes of RBAC 013 were added this way (System, Security and Website modules). The admin mirror list `apps/admin/src/auth/permissions.ts` must gain the same codes or its contract test fails.
- Dev admin account: `admin@melbournesphere.com` (renamed from `dev.admin@melbournesphere.local` on 2026-09-07; the Argon2id hash was not touched, so the existing password still works and is not recorded anywhere in the repo). A second account, `second.dev@melbournesphere.local`, is **disabled**. To obtain a working password, use the reset-link flow: POST `forgot-password` against an API instance you started (e.g. `PORT=3011 node apps/api/dist/main`) and read the link from Mailpit (http://127.0.0.1:8025) or process stdout with `MAIL_TRANSPORT=console`; afterwards run `pnpm --filter api auth:revoke-test-artifacts`.
- Dev data: published listings `runtime-check-cafe`, `carlton-corner-bakery`, `docklands-mobile-mechanics`; seeded taxonomy (`pnpm --filter api taxonomy:seed`).

## Security posture (implemented)
Argon2id passwords; opaque sessions (`ms_admin_session`, HttpOnly, SameSite=Strict, Path=/api/v1/admin, 30 min idle/12 h absolute); CSRF Origin guard; login/reset throttling (fail-safe 503); default-deny permissions guard; audit log; AES-256-GCM field encryption (`FIELD_ENCRYPTION_KEY`) for TOTP secrets and private enquiry emails; helmet strict CSP on the API; 64 KB body cap; env validation refuses insecure production values. Privileged admin mutations are metered (20/min, 200/h per administrator; per-process fallback during a Redis outage; `429` + `Retry-After`). Production refuses to start without verified MySQL TLS (`?sslmode=verify-ca|verify-identity`) or with `DATABASE_ALLOW_PUBLIC_KEY_RETRIEVAL=true`. Not yet: Turnstile/public form limits (16), retention jobs, web-tier TLS/CSP/headers (23–24).

## Database and migrations
Compose MySQL: databases `melbourne_sphere_dev`, `<dev>_shadow`, `melbourne_sphere_test` (integration harness truncates `APPLICATION_TABLES` in `apps/api/test/integration/harness.ts` — add new tables there). Policy: `utf8mb4_unicode_ci`, plural snake_case `@@map`, `scripts/check-migrations.mjs`. All 8 migrations applied to dev; test DB gets `migrate deploy` in the integration global setup.

## Known issues / deferred
- Admin bundle: main chunk 973 kB (298 kB gzip) after route/editor code splitting; antd + Refine dominate the remainder (NFR 013 review in Phase 23).
- A failed API read on a public page renders the designed error boundary with a real HTTP 500 (verified with the API stopped); a page whose data is still in the Next data cache is served from it instead. Curated and detail pages return real 404s.
- Cross-date overlap between a custom exception's overnight interval and the next day is not validated.
- "Open now" (DIR 008) is implemented and conditional: offered only when ≥60% of published listings publish hours and at least five do. On the dev data it reports `available: false` and answers unfiltered — that is the requirement working, not a gap.
- No approved public contact address: the general settings refuse a development domain and `SITE_CONTACT_EMAIL` is a `.local` value, so `contactChannel()` withholds it and the header's "Add a business" action points at the homepage explanation (`#business-listing`). Set a routable address and the mailto returns everywhere with no code change.
- About and the policy pages are not published, so they are absent from the navigation and footer by design (no link ever points at a 404); the review form still links to `/review-guidelines`, which 404s until that page exists. `/contact` always resolves and carries the contact form, but stays `noindex` until an editor publishes the approved copy.
- Mail locally: the ignored `apps/api/.env` now sets `MAIL_TRANSPORT=smtp` with `SMTP_HOST=127.0.0.1`, `SMTP_PORT=1025`; read every message at http://127.0.0.1:8025. **The API the client left running on 3001 is `start:prod`, not a watcher, so it still uses the console transport until they restart it** — it was deliberately not stopped. After any check that signs in or requests a reset link, run `pnpm --filter api auth:revoke-test-artifacts`; `auth:artifacts:check` must report zero.
- Public submissions (reviews, comments, business enquiries and the site contact form) are closed locally on purpose: no `TURNSTILE_SECRET_KEY` (API) or `TURNSTILE_SITE_KEY` (web) is configured, so the API answers 503 and the forms show a closed notice. Client keys are decision D03. For a runtime check, start a private API instance with Cloudflare's published test key and `PUBLIC_SITE_URL=https://example.com` (the hostname check rejects mismatches).
- Email provider account (D03b): the SMTP adapter is production-ready and verified locally; only the client's relay credentials, verified sender domain and provider-specific bounce webhooks (ENQ 006) remain.
- Admin specs render the page under test (not the whole route tree) except the nav-visibility tests; with `--maxWorkers=2` the suite is stable at about 36 s. Run single files while developing and the full check only at a phase gate.
- **Integration isolation (fixed, do not regress):** every integration file must bind one HTTP port through `listenForTests` (`test/integration/harness.ts`). A new suite that builds its app by hand and skips it reintroduces supertest's per-request `listen(0)`, whose `TIME_WAIT` port reuse makes one request read another's response (seen as a 401 on a public route, a 403 assertion receiving 401, or `Parse Error: Expected HTTP/`). `truncateApplicationTables` also carries an explicit 60 s transaction timeout for the same reason its absence broke four suites. Determinism after the fix: 5 consecutive full runs, a reversed-order run, and 5 repeats of the implicated trio, all green (174 tests).
- Earlier transient flake, unrelated and not seen since: Redis ping in readiness.

- Public visual system refreshed on 2026-09-07: Manrope body + Sora display, ink/mineral navy, teal/mist, warm ivory and restrained champagne accents; reusable glass and infographic primitives live in `packages/ui/src/styles.css` and are documented in `docs/design/public-ui.md`. Homepage is the first applied route; remaining public routes should follow one family at a time.

## Immediate next actions
1. **Client responses to `docs/launch/client-decisions.md`** (D01 boundary recommendation, D02 brand assets, D03 accounts, D04 domain/contact, D05 copy, D06 TOTP policy, D07 on-call, D07a staging, S01 SEO sign-off). Apply each as recorded; engineering follow-ups that unlock: boundary rules 5–7 (`melbourne-boundary.md` §6), bounce webhooks (D03b), error tracker (D03d), mandatory-TOTP gate (D06), WebSite SearchAction (P5).
2. Client review of the access-control audit and its closure addendum (`docs/audits/authorization-integration-audit.md`).
3. Manual screen-reader pass (the rest of the accessibility acceptance is automated: keyboard order, skip link, landmarks, headings and 320 px layout).
4. Run the Playwright journeys against staging once it exists (`E2E_WEB_URL`, `E2E_API_URL`, `E2E_ADMIN_URL`, and `E2E_ADMIN_EMAIL`/`E2E_ADMIN_PASSWORD` for the authenticated flows), and assemble the QA 003 evidence pack.
5. Production-shaped restore drill; the backup account needs `RELOAD`/`BINLOG ADMIN` so the binlog position is recorded (BACK 001 RPO).
6. Everything else outstanding is a client decision in `docs/launch/client-decisions.md` (the older list in `docs/pre-audit-report.md` §4 points there).

## Verification tooling added in Phase 25
- `e2e/` — Playwright journeys (`pnpm --filter @melbourne-sphere/e2e test`); needs the stack running, skips authenticated flows without credentials.
- `tools/load/` — `seed-load-data.mjs` (fills an isolated `*_load` database with the NFR 003 volume) and `run-load.mjs` (`--api` for the API mix, `--duration 1800` for the full acceptance run).
- `infrastructure/backup/` — backup and restore-drill scripts; they use `age` or `gpg`, whichever is installed.

## Files most relevant now
`packages/ui/src/styles.css` (all public tokens: colour, type, widths, spacing, radii, shadows, focus, motion), `apps/web/src/app/globals.css` (`.font-display`, `.ms-card-lift`, `.ms-prose`), `apps/web/src/components/page-shell.tsx` (`Band`/`SectionHeading`/`gridColumns`), `apps/web/src/app/**` and `apps/web/src/components/**` (public UI), `apps/admin/src/components/ui/*` (design-system primitives — reuse, do not re-invent per screen), `apps/admin/src/config/theme.ts`, `apps/admin/src/layouts/AdminShell.tsx`.

## Last successful validation (2026-09-08, Phases 35/36/40b/40c gate)
`pnpm check` green end to end: lint with **zero warnings**, typecheck, unit — database 27, API 252, admin 175, web 103, domain 12, mail 16, worker 33 — e2e 19, every build, bundle budget, contracts in sync. `pnpm test:integration` green: 35 files / 275 tests against the real MySQL and Redis, plus the database package's 5. Runtime: `/about` 200 with the navigation and footer links present, `/privacy` 404 while unpublished, `/contact` 200, `pages.xml` in the sitemap index, canonical/Open Graph/breadcrumb/`AboutPage` JSON-LD present, no console errors and no failed requests; measured at 1440/1024/768/390/320 px with no horizontal overflow. Note for the next session: the admin suite is capped at four workers (`apps/admin/vitest.config.ts`) — at one worker per core the interaction-heavy specs lose their async windows and fail on load rather than on code.

## Previous validation (2026-09-07, launch cleanup gate)
`pnpm check` green: database 27, API 188, admin 129, web 79, domain 12, mail 16, worker 19, e2e 19, all builds, bundle entry 776 kB, contracts in sync. Integration determinism after the port-isolation fix: 5 consecutive full runs at 174 passed, a reversed-order run, 5 repeats of the previously failing trio, and each file alone — all green. Runtime through the ignored `.env`: reset and enquiry mail in Mailpit as plain text with the visitor as Reply-To, one message for a replayed idempotency key, no token in the console. `auth:artifacts:check` reports zero. Secret scan clean.

## Previous validation (2026-09-07, launch-pack gate)
`pnpm check` green: database 27, API 188, admin 129, web 79, domain 12, mail 10, worker 19, e2e 19, all builds, bundle budget (entry 776 kB), contracts in sync. `pnpm test:integration` green on the confirming run (database 5, API 172); the first run had two order-dependent flakes (`reviews.integration-spec` "rating distribution" answered 401; `static-pages.integration-spec` "contact routing" hit `Parse Error: Expected HTTP/`), both green in isolation and on the rerun — unrelated to mail; watch for recurrence. Runtime: SMTP adapter verified end to end into Mailpit (password reset from the API; contact enquiries from the worker with Reply-To, stable `Message-ID`, `providerAccepted`, idempotent replay sending once; dead relay → `TransientMailError` in 5 ms). Secret scan clean.

## Previous validation (2026-09-07, Phase 29 gate)
`pnpm check` green: database 23, API 178, admin 117, web 74, domain 12, worker 16, e2e 19, all builds, bundle budget (entry 775 kB, total 2.21 MB), contracts regenerated and in sync. `pnpm test:integration` green: database 5, API 139, including a 21-case access-control suite against the real MySQL and Redis (effective-permission cases, 401/403 on routes reached directly, the privileged invariants, transactional and version-checked assignment, cache invalidation per trigger and a Redis outage that neither grants nor loses access). Browser: signed in through the reset-link flow with a temporary value — rotated and its session revoked afterwards — and checked the roles list, permission catalogue, role editor and administrator access editor at 1280 and 390 px; a role assignment saved through the confirmation dialog and its `authz.admin.roles` audit event read back. Axe over the role editor and access editor: zero violations. Earlier gates still stand: 34 Playwright journeys at desktop and 320 px with zero axe violations on the public site, and the Phase 25 capacity run (89,850 requests, 0 errors, p95 200 ms).
